export const enum Routes {
  Home = "/",
  Marketplace = "/marketplace",
  AboutUs = "/about-us",
  Login = "/login",
  Dashboard = "/dashboard",
  Signup = "/signup",
  Checkout = "/checkout",
}

export const enum AssetFilters {
  All,
  Models3D,
  AR,
  FullFrame,
}
